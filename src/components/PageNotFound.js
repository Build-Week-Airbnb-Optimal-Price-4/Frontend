import React from 'react';
import styled from 'styled-components';

const PageNotFoundContainer = styled.div`

`

const PageNotFound = () => {
    return (
        <PageNotFoundContainer>
            <p>PageNotFound component</p>
        </PageNotFoundContainer>
    );
};

export default PageNotFound;